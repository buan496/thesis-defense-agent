global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "thesis-defense-agent-api"
    metrics_path: "/metrics/prometheus"
    static_configs:
      - targets:
          - "api:8000"
