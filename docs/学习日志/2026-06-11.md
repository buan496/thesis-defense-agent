---
tags:
  - learning-log
  - agent
  - evaluation
date: 2026-06-11
---

# 2026-06-11 学习记录

## 今日完成

- 完成第二个工具 `create_defense_questions`
- 将两个 Tool Schema 同时提供给 LLM
- 完成工具注册表扩展
- 完成离线多工具链测试
- 完成真实模型多工具路由验证
- 新增 Agent 路由 benchmark
- 新增路由评估器和 `evaluate-agent-routing` CLI 命令
- 当前完整测试基线更新为 `56 passed`

## 本次真实评估

```text
TOTAL: 3
PASSED: 3
FAILED: 0
TOOL ROUTING ACCURACY: 1.0
```

覆盖的路由类型：

1. 普通问候：不调用工具
2. 论文事实问题：调用 `search_thesis`
3. 生成论文答辩问题：依次调用 `search_thesis` 和 `create_defense_questions`

## 核心概念

### Tool Routing

模型根据用户意图决定是否调用工具，以及调用哪个工具。

### Tool Chaining

前一个工具的结果成为后一个工具的输入。本项目中的工具链为：

```text
search_thesis
→ create_defense_questions
→ final answer
```

### 路由准确率

当前采用严格顺序匹配：

```text
actual_tools == expected_tools
```

工具相同但顺序不同，也会判定失败。

## 下一步

学习 Tool argument accuracy，评估模型不仅是否选对工具，还是否生成了正确、合法且符合用户意图的工具参数。
