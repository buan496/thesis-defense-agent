---
tags:
  - progress
  - agent
  - rag
status: active
updated: 2026-06-11
---

# 当前进度

## 当前项目能力

### LLM 与提示词

- [x] 使用 OpenAI-compatible SDK 调用 DeepSeek
- [x] 使用 `.env` 管理模型和密钥配置
- [x] Prompt Engineering 与约束性提示词
- [x] JSON 结构化输出与异常清洗
- [x] 答辩问题生成、回答评价、追问和答案改写

### 文档与 RAG

- [x] TXT 和 PDF 文档读取
- [x] PDF 非法 Unicode 与换行清洗
- [x] 段落切分、滑动窗口和 chunk metadata
- [x] SiliconFlow `BAAI/bge-m3` embedding
- [x] 余弦相似度和本地 JSON 向量库
- [x] 检索上下文、来源、Chunk ID 和相似度溯源
- [x] RAG 问答和 RAG 答辩问题生成
- [x] 检索 benchmark、`top_k` 对比与关键词召回评估
- [x] JSON 评估报告、时间戳报告和质量门禁
- [x] query embedding 缓存
- [x] 缓存与 embedding model 绑定
- [x] cache hits / misses 指标
- [x] 向量库断点恢复和周期 checkpoint
- [x] 向量库 metadata、一致性检查和 `--force` 重建

### Agent Harness

- [x] `search_thesis` 工具函数
- [x] Tool JSON Schema
- [x] 工具注册表和白名单
- [x] JSON 参数解析
- [x] LLM 自动选择工具
- [x] 工具执行结果以 `role="tool"` 返回模型
- [x] Agent Loop
- [x] 最大执行步数保护
- [x] 工具异常恢复
- [x] `AgentResult` 和 `ToolTrace`
- [x] 工具调用成功状态
- [x] 工具调用耗时 `duration_ms`
- [x] Agent Trace JSONL 保存函数与单元测试
- [x] Agent Trace 接入真实运行脚本
- [x] Trace 运行产物加入 `.gitignore`
- [x] Agent Trace 分析器
- [x] 空 Trace 与非法 JSONL 检查
- [x] `analyze-traces` CLI 命令
- [x] `top_k` 资源范围限制
- [x] LLM 与工具执行器依赖注入
- [x] Agent Harness 离线测试
- [x] 第二个工具 `create_defense_questions`
- [x] 多工具 Tool Schema 与工具注册表
- [x] `search_thesis → create_defense_questions` 工具链
- [x] Agent 工具路由 benchmark
- [x] 工具选择顺序与路由准确率评估
- [x] `evaluate-agent-routing` CLI 命令
- [x] Tool argument accuracy
- [x] 工具参数 JSON、必填字段与范围校验
- [x] 检索 query 关键词校验
- [x] 问题生成 context 的前置工具溯源
- [x] 路由、参数与整条任务准确率分离统计
- [x] 工具参数准确率质量门禁
- [x] Task completion rate
- [x] 最终回答非空检查
- [x] 最终回答关键词覆盖检查
- [x] 最低问题数量检查
- [x] 任务完成率与端到端成功率分离统计
- [x] 任务完成率质量门禁
- [x] Groundedness 关键声明与检索证据检查
- [x] 成功 `search_thesis` 结果作为唯一论文证据源
- [x] 声明覆盖率、Grounded task rate 与完整成功率
- [x] Groundedness CLI 质量门禁
- [x] Faithfulness Judge Prompt 与结构化 JSON 协议
- [x] Judge 输出清洗、字段校验和分数范围校验
- [x] 从 `ToolTrace` 提取成功的论文检索证据
- [x] `AgentResult → Evidence → LLM Judge` 评估链路
- [x] Faithfulness 分数、通过率和端到端成功率
- [x] Faithfulness CLI 输出与质量门禁
- [x] 独立 Faithfulness benchmark
- [x] Judge accuracy 计算与 CLI 质量门禁
- [x] 完整性与忠实度评分边界
- [x] Faithfulness Judge 重复运行稳定性评估器
- [x] 平均一致率、全票一致率与多数投票准确率
- [x] Judge 稳定性离线测试
- [x] `evaluate-faithfulness-stability` CLI 命令
- [x] Judge 稳定性三项质量门禁
- [x] 3 轮真实重复运行稳定性基线

### Agent 测试覆盖

- [x] 无工具时直接回答
- [x] 调用工具后生成最终回答
- [x] 工具失败后继续运行
- [x] 达到最大步数后停止
- [x] 拒绝未知工具
- [x] 拒绝非法 JSON 参数
- [x] 拒绝越界 `top_k`
- [x] 工具选对但参数错误的评估
- [x] 问题生成上下文未来源于检索结果的评估
- [x] 工具与参数正确但最终任务未完成的评估
- [x] 答案声明有检索证据的评估
- [x] 答案声明缺少检索证据的评估
- [x] 检索有证据但答案缺少声明的评估
- [x] 检索工具失败时 Groundedness 失败
- [x] Faithfulness Judge 合法与非法 JSON 测试
- [x] 无检索证据时跳过 Judge 并判定失败
- [x] Faithfulness 通过与语义矛盾集成测试

## 当前测试基线

```text
100 passed
```

最近验证日期：2026-06-12。

## 当前代码断点

已修改但可能尚未提交的文件包括：

```text
app/agent.py
app/agent_models.py
app/tools/definitions.py
app/tools/thesis_search.py
scripts/test_agent.py
scripts/test_tool_call.py
tests/test_agent.py
tests/test_thesis_search_tool.py
tests/test_tool_definitions.py
tests/test_tool_executor.py
```

开始下一阶段前应执行：

```powershell
uv run pytest
git status
```

确认无误后提交本阶段。

## Faithfulness 报告持久化

已完成：

- 保存单次 Faithfulness benchmark JSON 报告
- 保存多轮稳定性 JSON 报告
- 记录 Judge 模型、评估时间和运行配置
- 保存每个案例的详细判断理由
- 保存稳定性评估中每一轮的完整结果
- 支持 `--output` 指定报告路径
- 支持 `--save-report` 自动生成带时间戳的报告
- 在质量门禁判断前保存报告，失败现场不会丢失

真实报告验证：

```text
Faithfulness accuracy: 1.0
Stability repeat_count: 3
Average agreement: 1.0
Unanimous rate: 1.0
Majority accuracy: 1.0
```

报告目录：

```text
data/reports/
```

该目录已加入 `.gitignore`，运行产物不会进入版本库。

## 评估报告回归对比

已完成：

- 加载并校验 baseline 与 current 报告
- 检查两份报告的评估类型是否一致
- 比较 Faithfulness accuracy
- 比较平均一致率、全票一致率和多数投票准确率
- 按案例名称对齐结果，不依赖列表顺序
- 检测正确预测变为错误预测
- 检测多数投票结果翻转
- 检测单案例一致率下降
- 检测全票一致变为不一致
- 记录新增与删除的 benchmark 案例
- 记录 Judge 模型和配置变化
- 保存 JSON 对比报告
- 支持发现回归时返回退出码 1

CLI 命令：

```powershell
python -m app.cli compare-evaluation-reports `
  --baseline data/reports/baseline.json `
  --current data/reports/current.json `
  --fail-on-regression `
  --save-report
```

当前真实报告自比较结果：

```text
REGRESSION COUNT: 0
HAS REGRESSION: False
REGRESSION STATUS: PASS
```

## 下一步

### 当前唯一任务

将 CI 设为分支保护的 Required Status Check：

- 将当前分支合并到默认分支
- 在 GitHub 上首次运行离线 CI
- 检查并下载 Artifact
- 配置 Branch Protection
- 要求 `Offline tests and quality gate` 通过后才能合并
- 手动运行一次 Online Evaluation

## GitHub Actions 离线 CI

已完成：

- 新增 `.github/workflows/ci.yml`
- Push、Pull Request 和手动触发时运行
- 使用 Python 3.13 与锁定的 `uv.lock`
- 开启 uv 依赖缓存
- 运行全部离线单元测试
- 生成 pytest JUnit XML
- 执行离线评估报告回归门禁
- 保存 JSON 与 Markdown 回归报告
- 使用 Artifact 保存 14 天
- 使用并发控制取消同一分支的旧任务
- 将权限限制为只读仓库内容
- 默认 CI 不接触真实 API Key

质量门禁退出码：

```text
无回归 → exit code 0
检测到回归 → exit code 1
```

本地复现结果：

```text
100 passed
REGRESSION COUNT: 0
REGRESSION STATUS: PASS
```

Workflow YAML 已通过 `yamllint`。

## GitHub Actions 在线评估

已完成：

- 新增 `.github/workflows/online-evaluation.yml`
- 只支持手动 `workflow_dispatch`
- 使用 Repository Secret `DEEPSEEK_API_KEY`
- 支持选择是否运行稳定性评估
- 将稳定性重复次数限制为 2 或 3
- 可选择各项质量门禁阈值
- 单次评估失败后仍可继续收集稳定性报告
- 使用最终门禁步骤统一决定 Workflow 状态
- 成功或失败时都上传 JSON 报告
- 报告 Artifact 保留 14 天
- 限制仓库权限为只读
- 不在 Push 或 Pull Request 中使用真实密钥
- 缺少 Secret 时立即给出明确错误

预计 API 调用次数：

```text
仅 Faithfulness：4 次
Faithfulness + 稳定性 2 轮：12 次
Faithfulness + 稳定性 3 轮：16 次
```

必须在 GitHub 仓库中配置：

```text
Settings
→ Secrets and variables
→ Actions
→ New repository secret
→ DEEPSEEK_API_KEY
```

手动 Workflow 文件必须进入默认分支后，GitHub Actions 页面才会显示运行按钮。

下一阶段将继续学习：

- Branch Protection 与 Required Status Checks
- Workflow 运行日志排查
- Artifact 下载与报告复核
- CI 状态如何阻止有问题的 PR 合并
